import React from "react";
import Break from "../Break/Break";

const Calculate = ({count}) => {
  return (
    <div>
      <Break count={count}></Break>
    </div>
  );
};

export default Calculate;
